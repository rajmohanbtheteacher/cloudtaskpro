
CloudTaskPro Full App Setup - Phase 2

✅ React Frontend (Glass UI):
   - Beautiful login page (glass effect)
   - Dashboard to view/create/delete tasks
   - Axios integration with backend

✅ Flask Backend (JWT + MongoDB):
   - User registration & login with JWT token
   - MongoDB integration (containerized)
   - CRUD for tasks

✅ Docker:
   - Dockerfile for backend
   - Dockerfile for frontend
   - docker-compose.yml with MongoDB service

✅ CI/CD:
   - Jenkinsfile for build + test + deploy
   - GitHub repo (manually configured)
   - Jenkins Docker container support

🔄 Kubernetes (optional):
   - Helm chart (pending)
   - ArgoCD integration (pending)

Notes:
- MongoDB running on local container
- JWT secret stored securely via .env
